{-# LANGUAGE NoImplicitPrelude #-}

module AI.GP.Instance.Init.Full where

import AI.GP.Class.InitMethod (InitMethod, init)
import AI.GP.Type.Class.Init.Full (Full)

instance InitMethod Full where
    init _full = let a=a in a
